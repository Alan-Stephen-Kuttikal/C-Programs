#include<stdio.h>
int main()
{
	int n,i,c;
	c=0;
	printf("Enter the number:");
	scanf("%d",&n);
	for(i=2;i<=n/2;i++)
	{
		if(n%i==0)
		{
			printf("%d is not Prime",n);
			c=0;
			break;
		}
		else
			c=1;
	}
	if(c==1)
		printf("%d is a prime number",n);
	return 0;
}
