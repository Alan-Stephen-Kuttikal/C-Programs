#include<stdio.h>
int main()
{
	int r;
	float p,a;
	printf("Enter the radius:");
	scanf("%d",&r);
	a=3.14*r*r;
	p=2*3.14*r;
	printf("Area of circle is %f\nPerimeter of circle is %f",a,p);
	return 0;
}
