#include<stdio.h>
int main()
{
	int n,r,c;
	printf("Enter a number:");
	scanf("%d",&n);
	c=n;
	r=0;
	while(n!=0)
	{
		r=r*10+n%10;
		n=n/10;	
	}
	if(c==r)
		printf("%d is a palindrome",c);
	else
		printf("%d is not a palindrome",c);
	return 0;
}
