#include<stdio.h>
#include<math.h>
int main()
{
	int a,b,max;
	printf("Enter first number:");
	scanf("%d",&a);
	printf("Enter second number:");
	scanf("%d",&b);
	max=(a+b+abs(a-b))/2;
	printf("Maximum of %d and %d is %d",a,b,max);
	return 0;
}
